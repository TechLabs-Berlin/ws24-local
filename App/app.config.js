module.exports = {
    // ... other Expo configurations (if you have any)
    svg: {
      expo: {
        // Replace with the actual path to your SVG assets directory during build
        subdirectory: 'assets/images', // Adjust this path if your SVGs are in a different location
      },
    },
  };